import React from "react";
import styled from "styled-components";
import Card from "@material-ui/core/Card";

export const StyledCard = styled(Card)`
  padding: 15px;
`;

export const EmojiDisplay = () => {
  return <StyledCard>SIKE</StyledCard>;
};
